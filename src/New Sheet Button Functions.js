/**
 * After user clicks "New Sheet" on an attendance tab:
 * 
 * Create a new, usable atteandance sheet: createAttendanceTab();
 * 
 * Add students from registration tab in ascending order (max 75 students)
 */

function newSheetButton() {

  //Create a new, usable attendance sheet
  createAttendanceTab();

  //Add students from registration tab in ascending order (max 75 students)
  addStudents();
  
}
